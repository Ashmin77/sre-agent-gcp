# Python
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Environment files — copy from .env.example and fill in your values
agent/.env

# Terraform state — contains sensitive secrets (CA certs, endpoints, keys)
# Use remote state (GCS backend) for team deployments
iac/terraform.tfstate
iac/terraform.tfstate.backup
iac/.terraform/

# Terraform variable overrides — copy from terraform.tfvars.example and fill in your values
iac/terraform.tfvars

# macOS
.DS_Store

# IDE
.vscode/
.idea/

# Archives
*.zip
